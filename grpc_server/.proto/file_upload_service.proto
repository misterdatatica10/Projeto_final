syntax = "proto3";

service FileUploadService {
  // Definindo o serviço gRPC para upload de arquivos
  rpc UploadFile (stream FileChunk) returns (UploadStatus);
}

// Definindo a mensagem para o arquivo em pedaços
message FileChunk {
  bytes content = 1;      // Conteúdo do arquivo em partes (em binário)
  string file_name = 2;   // Nome do arquivo, enviado no primeiro pedaço
}

// Definindo a resposta de status do upload
message UploadStatus {
  bool success = 1;
  string message = 2;
}
